[tools]
actionlint = "1.7.12"
jq = "1.8.2"
shellcheck = "0.11.0"

[tasks.package-update]
run = "scripts/package-update.sh"

[tasks.check]
run = "actionlint && shellcheck scripts/package-update.sh"

[settings]
lockfile = true
